name: MotorImageryDemo
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy
  - scipy
  - scikit-learn
  - mne
  - joblib
  - pyyaml
  - pytest
  - pip
  - pip:
      - pylsl
      - -e .
